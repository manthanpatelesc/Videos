<?xml version="1.0" encoding="UTF-8"?><!--
  ~ Created on 5/3/19 3:25 PM.
  ~ Copyright © 2019 刘振林. All rights reserved.
  -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="false">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />

            <!-- 设置大小时，应注意Switch显示时UI内容的高度会以此高度为依据 -->
            <size android:width="25dp" android:height="25dp" />

            <!-- 添加一个边，使UI更好看；当false时为未选择状态的底图颜色，true时为选择状态的底图颜色。-->
            <stroke android:width="2dp" android:color="@color/lighter_gray" />
        </shape>
    </item>

    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
            <size android:width="25dp" android:height="25dp" />
            <stroke android:width="2dp" android:color="?attr/colorAccent" />
        </shape>
    </item>
</selector>
